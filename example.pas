program HelloWorld;
var
    x, y, z: integer;
begin
    not and or
    >= <= < > <>
    x == x
    x := 4 / 2;
    z := x + 1;
    y := 12 - z;
    writeln("Hello World!");
    writeln(x);
    writeln(y);
    writeln(z);
end.
